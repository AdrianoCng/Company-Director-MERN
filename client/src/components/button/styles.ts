import styled from "styled-components";

export const Button = styled.button`
    // position: absolute;
    // right: 20px;
    // bottom: 20px;
    padding: 10px;
    background-color: var(--primary);
    color: white;
    border: none;
    border-radius: 5px;
    font-size: 1.04em;
    font-weight: bold;
    min-width: 100px;
    cursor: pointer;
`;
